# Module bluetape4k-aws-kotlin

bluetape4k용 AWS Kotlin SDK 통합 모듈이다. native suspend helper, request
builder, batch utility, service extension을 제공하여 호출자가
`CompletableFuture` bridge 없이 coroutine 코드에서 AWS 서비스를 직접 사용할 수
있게 한다.
