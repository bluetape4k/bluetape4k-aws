# Module bluetape4k-aws-kotlin

AWS Kotlin SDK integration module for bluetape4k. It provides native suspend
helpers, request builders, batch utilities, and service extensions so callers can
use AWS services directly from coroutine code without `CompletableFuture`
bridges.

