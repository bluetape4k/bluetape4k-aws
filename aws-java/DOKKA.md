# Module bluetape4k-aws-java

bluetape4k용 AWS Java SDK v2 통합 모듈이다. AWS SDK 모델 타입을 그대로
노출하면서 DynamoDB, S3, SES, SNS, SQS, KMS, CloudWatch, Kinesis,
EventBridge, STS, Secrets Manager, Parameter Store에 대한 동기 helper,
비동기 `CompletableFuture` 확장, coroutine adapter를 제공한다.
