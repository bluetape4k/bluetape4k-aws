# Module bluetape4k-aws-spring-boot

AWS Java SDK v2 client와 coroutine-first 작업을 위한 Spring Boot 4
auto-configuration 모듈이다. runtime에서 awspring에 의존하지 않으면서
template, listener infrastructure, remote environment source, CloudWatch
helper, AWS 기반 Exposed database wiring을 제공한다.
