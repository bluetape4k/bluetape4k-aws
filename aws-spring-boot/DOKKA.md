# Module bluetape4k-aws-spring-boot

Spring Boot 4 auto-configuration module for AWS Java SDK v2 clients and
coroutine-first operations. It provides templates, listener infrastructure,
remote environment sources, CloudWatch helpers, and AWS-backed Exposed database
wiring without depending on awspring at runtime.

